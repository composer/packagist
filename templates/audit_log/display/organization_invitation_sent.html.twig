{% import 'audit_log/macros.html.twig' as auditLog %}

Invitation sent to <strong>{{ display.email }}</strong> for <strong>{{ auditLog.organizationLink(display.organization) }}</strong><br>
Invited by: {{ auditLog.userLink(display.actor.username) }}
