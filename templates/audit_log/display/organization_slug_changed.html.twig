{% import 'audit_log/macros.html.twig' as auditLog %}

<strong>{{ auditLog.organizationLink(display.organization) }}</strong>
<strong>{{ display.slugFrom }}</strong> → <strong>{{ display.slugTo }}</strong><br>
Changed by: {{ auditLog.userLink(display.actor.username) }}
