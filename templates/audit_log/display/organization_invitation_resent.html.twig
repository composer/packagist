{% import 'audit_log/macros.html.twig' as auditLog %}

Invitation to <strong>{{ display.email }}</strong> resent for <strong>{{ auditLog.organizationLink(display.organization) }}</strong><br>
Resent by: {{ auditLog.userLink(display.actor.username) }}
