{% import 'audit_log/macros.html.twig' as auditLog %}

<strong>{{ display.member.username }}</strong> joined {{ display.teamNames|length > 1 ? 'teams' : 'team' }} <strong>{{ display.teamNames|join(', ') }}</strong> in <strong>{{ auditLog.organizationLink(display.organization) }}</strong> (via invitation)<br>
