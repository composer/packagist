{% import 'audit_log/macros.html.twig' as auditLog %}

Invitation to <strong>{{ display.email }}</strong> revoked for <strong>{{ auditLog.organizationLink(display.organization) }}</strong><br>
Revoked by: {{ auditLog.userLink(display.actor.username) }}
