{% import 'audit_log/macros.html.twig' as auditLog %}

<strong>{{ auditLog.packageLink(display.packageName) }}</strong><br>
{{ display.list.value | capitalize }} entry for versions {{ display.version }} reported by {{ display.source.displayName }} was enabled by {{ display.actor.username }}.
