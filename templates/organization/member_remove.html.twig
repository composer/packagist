{% extends 'organization/layout.html.twig' %}

{% block title %}Remove member - {{ organization.displayName }} - {{ parent() }}{% endblock %}

{% block org_content %}
    <h3 class="title">Remove "{{ member.username }}" from {{ organization.displayName }}</h3>

    <p>This removes the member from the organization and all of its teams. This action cannot be undone.</p>

    {{ form_start(form, { 'attr': { 'class': 'organization_member_remove' } }) }}
        {{ form_errors(form) }}
        <input class="btn btn-danger" type="submit" value="Remove member" />
        <a class="btn btn-default" href="{{ path('organization_members', { organization: organization.slug }) }}">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
