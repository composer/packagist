{% extends "layout.html.twig" %}

{% block title %}Create an organization - {{ parent() }}{% endblock %}

{% block content %}
    <h2 class="title">Create an organization</h2>

    <div class="row">
        <div class="col-md-8">
            {{ form_start(form, { 'attr': { 'class': 'organization_details' } }) }}
                {{ form_errors(form) }}

                <div class="form-group clearfix">
                    {{ form_label(form.displayName) }}
                    {{ form_errors(form.displayName) }}
                    {{ form_widget(form.displayName) }}
                    {{ form_help(form.displayName) }}
                </div>

                <div class="form-group clearfix">
                    {{ form_label(form.slug) }}
                    {{ form_errors(form.slug) }}
                    {{ form_widget(form.slug) }}
                    {{ form_help(form.slug) }}
                </div>

                <input class="btn btn-success btn-lg" type="submit" value="Create organization" />
            {{ form_end(form) }}
        </div>
    </div>
{% endblock %}
