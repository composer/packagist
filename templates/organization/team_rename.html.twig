{% extends 'organization/layout.html.twig' %}

{% block title %}Rename team - {{ organization.displayName }} - {{ parent() }}{% endblock %}

{% block org_content %}
    <h3 class="title">Rename team "{{ team.name }}"</h3>

    {{ form_start(form, { 'attr': { 'class': 'organization_team_rename' } }) }}
        {{ form_errors(form) }}
        <div class="form-group clearfix">
            {{ form_label(form.name) }}
            {{ form_errors(form.name) }}
            {{ form_widget(form.name) }}
            {{ form_help(form.name) }}
        </div>
        <input class="btn btn-success" type="submit" value="Rename team" />
        <a class="btn btn-default" href="{{ path('organization_teams', { organization: organization.slug }) }}">Cancel</a>
    {{ form_end(form) }}
{% endblock %}
