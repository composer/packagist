{% extends "user/layout.html.twig" %}

{% block user_content %}
    {{ form_start(form, { 'action': path('change_password'), 'attr': { 'class': 'change_password col-lg-6', 'id': 'change_password' } }) }}
        {{ form_errors(form) }}

        <div class="form-group clearfix">
            {{ form_label(form.current_password) }}
            <div class="input-group">
                <span class="input-group-text"><span class="icon-key"></span></span>
                {{ form_widget(form.current_password) }}
            </div>
            {{ form_errors(form.current_password) }}
        </div>

        <div class="form-group clearfix">
            {{ form_label(form.plainPassword) }}
            <div class="input-group">
                <span class="input-group-text"><span class="icon-lock"></span></span>
                {{ form_widget(form.plainPassword) }}
            </div>
            {{ form_errors(form.plainPassword) }}
        </div>

        {{ form_widget(form) }}

        <input type="submit" class="btn w-100 btn-inverse btn-lg" value="Change password" id="_change_password_submit" />
    {{ form_end(form) }}
{% endblock user_content %}
