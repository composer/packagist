{% extends 'user/layout.html.twig' %}

{% block title %}Reset your password - {{ parent() }}{% endblock %}

{% block user_content %}
    {% for flashError in app.flashes('reset_password_error') %}
        <div class="alert alert-warning" role="alert">{{ flashError }}</div>
    {% endfor %}

    {{ form_start(requestForm, {attr: {class: "request_pwd_reset col-lg-6", id: "request_password"}}) }}
        <div class="form-group">
            {{ form_label(requestForm.email) }}
            <div class="input-group">
                <span class="input-group-text"><span class="icon-user"></span></span>
                {{ form_widget(requestForm.email) }}
            </div>
        </div>

        <div><small>Enter your email address, and we will send you a link to reset your password.</small></div>

        <button class="btn w-100 btn-inverse btn-lg" id="_request_password_submit">Send password reset email</button>
    {{ form_end(requestForm) }}
{% endblock %}
