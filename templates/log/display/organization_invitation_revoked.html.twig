{% import 'log/macros.html.twig' as log %}

Invitation to <strong>{{ display.email }}</strong> revoked for <strong>{{ log.organizationLink(display.organization) }}</strong><br>
Revoked by: {{ log.userLink(display.actor.username) }}
