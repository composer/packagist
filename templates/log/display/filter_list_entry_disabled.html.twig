{% import 'log/macros.html.twig' as log %}

<strong>{{ log.packageLink(display.packageName) }}</strong><br>
{{ display.list.value | capitalize }} entry for versions {{ display.version }} reported by {{ display.source.displayName }} was disabled by {{ display.actor.username }}.
